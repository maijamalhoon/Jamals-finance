# Asset status

The SVG files under `public/brand/` are temporary integration assets, not the final trademark artwork.

They prove that web, PWA, native generators, and future marketing surfaces can consume replaceable paths from one brand manifest. The final custom JALVORO symbol and icon family can overwrite these files while retaining the same filenames and application references.
