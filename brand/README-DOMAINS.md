# Domain policy

The current Vercel hostname remains a compatibility fallback until a verified JALVORO domain is connected. Canonical origins are environment-specific and must not be embedded in the public brand manifest.
