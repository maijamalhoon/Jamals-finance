# GitHub boundary

GitHub is the versioned source of truth for the master platform brand. Every public brand revision is reviewed in a dedicated branch and previewed before merge. Technical compatibility aliases remain documented rather than silently replaced.
