# Security boundary

Brand configuration contains public identity only. Secrets, Supabase service-role credentials, Sentry auth tokens, payment keys, and private customer branding data must never be stored here.
