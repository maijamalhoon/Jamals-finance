# Vercel boundary

Vercel stores environment-specific canonical URLs and support addresses. Public name, tagline, product labels, and asset paths remain version-controlled so preview deployments can validate every brand revision before production.
